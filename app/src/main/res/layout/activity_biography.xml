<?xml version="1.0" encoding="utf-8"?>
<!--
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

</androidx.constraintlayout.widget.ConstraintLayout>
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
android:layout_width="match_parent"
android:layout_height="match_parent"
android:padding="16dp">

<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Image principale -->
    <ImageView
        android:id="@+id/image_johnny"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:scaleType="centerCrop"
        android:src="@drawable/johnny_hallyday_tete" />

    <!-- Titre -->
    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Johnny Hallyday"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center" />

    <!-- Sous-titre -->
    <TextView
        android:id="@+id/subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Un monument de la musique française"
        android:textSize="18sp"
        android:gravity="center"
        android:textStyle="italic" />

    <!-- Biographie -->
    <TextView
        android:id="@+id/biography"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Johnny Hallyday, né Jean-Philippe Smet le 15 juin 1943 à Paris et décédé le 5 décembre 2017 à Marnes-la-Coquette, était un chanteur, compositeur et acteur français. Il est considéré comme une icône du rock et une légende de la musique en France, avec une carrière de plus de 50 ans."
        android:textSize="16sp"
        android:lineSpacingExtra="4dp" />

    <!-- Autres images -->
    <ImageView
        android:id="@+id/image_concert"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginTop="16dp"
        android:scaleType="centerCrop"
        android:src="@drawable/johnny_concert" />

    <TextView
        android:id="@+id/legend"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Johnny Hallyday en concert - une expérience légendaire"
        android:textSize="14sp"
        android:gravity="center"
        android:textStyle="italic" />

</LinearLayout>
</ScrollView>
